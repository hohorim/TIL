apiVersion: v1
kind: Service
metadata:
  name: headless-service
  namespace: leehorim
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    app: nginx-svc
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80