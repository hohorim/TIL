* JS
 https://ko.javascript.info/

[10/11]
- V8: Chrome 브라우저엔 '자바스크립트 가상 머신’이라 불리는 엔진이 내장. 
	현재는 V8로 거의 엔진 정착.

- typeScript는 js로 트랜스파일되어 빌드됨.
- 주석은 왜 이 로직이 필요한지 적는 것.
- 블록 스코프: 변수를 접근할 수 있는 범위
- var: 블록이나 루프수즌의 스코프를 형성하지 않음. 힘수 스코프.
	즉 전역, 지역변수가 따로 존재하지 않음.
  let은 블록 스코프.
- const: 변하지 않는 변수 선언 시 사용. 상수 등.

- 자료형 변수에 대해.  
	. 자료형 변수로 나눠준 이유는 메모리를 유용하게 사용하기 위한 것.
	. Number.isSafeInteger: 1씩 증가할 수 있는 애인지 판단하는 함수.

- console 테스트
	0.1+0.2==0.3 -> false
	0.3-0.1==0.2 -> true
	
	컴퓨터는 이진수로바꿔 처리함. 소수점은 이진수로 처리할 때는 어림수로 떨어짐.
	즉, 소수점은 비교하면 안된다.
	
	js에는 이러한 비교를 지원해주는 함수가 존재한다.
	 > Number.EPSILON
	 > 이진수의 오차범위
	 > 속도 느려짐

[10/13]
- null: 자료형은 object, 값이 없음
- NaN: 숫자타입인데 숫자가 아니다.
- 연산자 > 조건문 > 함수.. 빠른순
- if? 가 if()보다 더 빠름
- ++a 가 a++ 보다 성능 좋음. 이전값을 저장하는 메모리를 필요로하지 않아서. 
- OR(||) 로 쓸 때 앞에가 참이면 뒤까지 작업하지 않음. 그래서 보통 참값을 앞쪽에 넣어주면 성능이 좋음.
  AND(&&) 도 false 값을 앞에 쓰면 성능 좋음.
- 함수 && 함수: 함수 호출할 때 보통 많이 쓰이는 방법.
 ex. fun1()&&func2() 이런식으로.
- !!("string"); //true
	!! 연산자는 boolean 형으로 바꿔서 출력해줌
- ~~ ->비트 not. int는 4바이트라서 2**31를 넘어간 숫자에 not(~~)을 취하면 1/-1 으로나오고 그 이전 값이면 일반 숫자나옴.

[10/18]
- .?: null이면 null return. ex) a.?
- 테스트개발환경경로: D:\jsTest\javascript 기본교육. 
- vscode 실행: alt + l + o 
- 정의: 어떠한 것에 대한 설명. 
  선언: 어떠한 것을 실행하는 것.
  
  function a() : 정의만. 메모리에 올라가지 않은 상태
	let b = new a() : 정의만 된 상태
  let a = {} : 정의와 실행이 된 상태. 메모리에 올라간 상태

	function 의 this 는 호출될 때 결정
	람다함수의 this는 선언될 때 결정
	
	정의
	function sayHi() {
	  alert( "Hello" );
	}

	정의와 선언
	//sayHi("John"); // error!
	let sayHi = function() {
	  alert( "Hello" );
	};
	위를 호출한 순간 선언됨. 실행이 될때 선언됨.
	그래서 코드 실행순간 정의만 됨으로 아래 sayhi를 함수 만들기전 위에 코드를 쓰면 에러남. 정의만 되었기때문이며,
	호출순간 선언과 실행이 같이 되어 에러남.
	
	아래표현이 더 나음.
- 실행전 정의와 선언이 되어야 호출 가능.

- 람다함수==익명함수
- 람다함수 파라미터가 1개일 때 굳이 return 이 없어도 값이 리턴됨. 단 2개 이상(중괄호를 사용했다면)이면 return 필요.

* i = i ? i < 0 ? Math.max(0, len + i) : i : 0;	
	-> 이거 찾아서 쓰지 않기. 없애자
	
- 의미: _[변수] private
		__[변수] 엔진 단에서 사용.

- 솔리드 5가지 원칙.
- 폴리필. String.prototype

[10/20]
- 리터럴({}):값이 되어 메모리에 올라가는 것. 중괄호({})를 이용해 객체 생성
	let a = {} => 객체 리터럴..
- 프로퍼티: 값을 가진 것.
- 메소드: 어떠한 기능을 하는 것.

- function 내의 this: 호출하는 시점에 this가 결정됨.
- 람다 함수 내의 this: 선언하는 시점에 this가 결정됨

[10/25]
- 옵셔널 체이닝:
	let a={a:'a',b:'b'}
	a.a -> 이거를 a?.a 처럼 바꾸자
	이는 옵셔널 체이닝 이라고 a가 있는지 먼저 확인하고 출력함. 없으면 undefined 나와서 더 안전하게 호출할 수 있음. 
	a?.c?.() 라 해도 에러가 나지 않고 undefined 출력되서 안전함.
	차이점은 디버깅이 어려워짐 에러가 나지 않아서. 또한 더 오래걸림. 
	undefined 와 null 만 평가함

- Symbol:
  Symbol('a'):  하나만 생성 가능. Symbol('a') === Symbol('a') (false)
	
  Symbol.for('a') : 전역변수처럼 여러개 만들 수 있음 Symbol('a') === Symbol('a') (true)
  
[10/27]
- 래퍼 객체:
	-> 원시 값: 스택에 올라와 있는 값.
	-> 정수나 스트링 등.
	-> 원시 자료형들을 임의로 감싸는 객체를 "래퍼 객체".
	-> 원시 값도 객체처럼 사용할 수 있도록. 원래는 (.) 해서 객체처럼 사용하지 못함. 그치만 임시 래퍼 객체로 인해 감싸졌다가 사용한 후에 버리게 됨. 
	-> 개발자 편의를 위해

- Number vs parseInt: String to Int
	-> 보통 같은 경우에 Number로 형 변환해줌
	-> parseInt는 특수한 경우. 단위까지도 변환해주는 등 더 기능이 큼.

- let a = 1000_0000; 
	-> 이런 식으로 써도 10000000 로 값이 저장됨.
	-> a = 10000000 식임.
	-> 소수도 가능.

- let a = 2e3
	-> a = 2000
- let a = 1234e-3 
	-> a = 1.234

- 123..toString(n) 
	-> 숫자를 n진수로 변환
	-> .. 이 두개인 이유는 소숫점 때문에
	

- js 에서는 최대한 undefined 사용. null 보다.
	-> Number(null) == 0 으로 떨어지기 때문에. undefined 사용추천

- 빠르게 형변환 string to number:
	-> +'10
- string to array:
	->  let str = [...a];
	
~~ 문자열부터	

[11/01]
- js 의 배열은 HashMap 처럼 이루어짐. key: value 형태

[11/03]
- for(int i in a) 증감연산자 없이 i는 index 로 나옴
	for(int i of a) 증감연사자 없이 i는 바로 해당 인덱스의 값이 나옴. 또한 객체에 대한 파라미터는 for of를 쓸수가 없음.
	
- 유사 배열: 
	브라우저 기능(getElement etc), 내장(arguments etc) 기능은 forEach 지원 x
	Array.from(배열) 로 배열처럼 만들어서 사용가능

[11/15]
	- ''.padStart(arg,arg) : 아주 유용할듯.
	- 재귀함수 많이 느림.
	
- 함수심화학습 목차 끝나면 개인 프로젝트 진행할 거임.

[11/22]
- 즉시실행함수:
	(function (){
	console.log();
  }());
	(function (){
	console.log();
  })()
  둘다 실행인데
	(function (){
	console.log();
  }(), funtion (){
	console.log();
  }()); 이거는 실행이 둘다 되는데
	(function (){
	console.log();
  }, function (){
	console.log();
  })(); 이거는 마지막 함수만 실행됨
  
-  sayHi() {
    // ...
  },

  sayBye: function() {
    // ...
  }: 차이가 있음 *찾아보기
  
- call/apply: 내꺼가 아닌 남의 것을 빌려쓸 때
	1234.substr(1,1) -> x
	''.substr.call(1234,1,1) -> o
	
- 함수가 호출될 될 때 this 지정
  
- this:
	let 은 블록스코프라 전역변수로 찍히지 않음
	제일 위에 var a = 1; 
	
[11/29]
- 상속:
	let a = {a:1}
	let b = {b:2}
	b.__proto__ = a : b의 부모에 a 를 넣어줌
	
	인스턴스(객체)에는 __proto__ 로 부모를 지정.
	클래스(function) 에는 prototype 으로 부모를 지정.
	
	* 오버라이딩 할 때 자식 선언 때 부모오버라이딩 후 다시 자식에 부모를 상속받으면? 엎어씌어지나 아니면 그대로인가 테스트
	* class 상속 챕터 읽기
	
[12/06]
- static: 메모리에 자리를 선점하고 있어, 호출될 때마다 항상 같은 메모리에 위치함
- Dynamic: 호출할 때마다 메모리 위치가 변함.

- 동기/비동기: 동기-순차적으로 한줄 씩 실행. 비동기-동시에 실행
	setTimeout: 비동기식. 모든 로직이 다 끝난 후 마지막에 setTimeout이 실행됨.
	동기로 실행되는 함수는 그 함수가 끝날 때까지 대기해야함. 끝나지 않으면 무한정 대기에 걸림.
	비동기는 실행되는 동안 다른 일도 가능할 수 있도록 함.
	
	
[12/27]
- promise 는 new 하면 동기인데 resolve 실행 시 비동기로 로직타게되어있음


[1/3]	
- 프로미스화: 콜백을 받는 함수를 프라미스를 반환하는 함수로 바꾸는 것
- await: await 안 함수안에서는 동기로 순서대로 실행되지만,
	await 붙인 함수 밖에서는 상관없이 비동기로 실행됨.
	예시: javascript 교육> 0105.html 참고.