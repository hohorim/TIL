# java 전사교육

- 스트링 인스턴스(인턴)
 스트링풀
 
- 삼항연산자는 모든 형을 다 맞춤.
- java 는 자료형으로 unsinged 를 표현할 수 없음.
- 비트연산은 정수형만 가능. 시프트 연산 등.
- double형은 비트위치와 표현이 다르다. -> 64bit 는 비트로 표현할 수 없음. 64 bit 이후의 비트자리는 연산 시 잘리니 값이 정확하지 않음.
	2^31 -1   +   2^33   == 2^31  -1 
	2^63 -1   +   2^63   != 2^63  -1 
	
	
	4,5 장 ~2일
	과제 ~7일
	
- for(초기화;조건식;증감식) 과 for(타입 변수명: 배열 또는 컬렉션) 은 성능차이:
	iterator
	전자는 다음인덱스를 사용자가 지정가능
	후자는 컬렉션이 다음인덱스를 야기함
	전자가 더 빠르긴함. iterator 가 개입하기 때문에. 해봤자 0.1% 정도.

- 시간복잡도 :
	빠르기 (log 밑진수는 2임)
	O(1) > O(logN) > O(n) > O(n^2)
	
	시간복잡도 풀 때는 데이터 갯수를 확인하고 해양함
	예를 들어 10개의 데이터가 포문을 돌 때 100개의 데이터가 나와야하면 n^2
		사전처럼의 8개의 데이터 포문 돌 때는 logN
		
- switch 문을 컴파일 할 때 컴퓨터에서 case 문을 트리형태 구조로 저장함. 그래서 속도가 빠름.
- 여러 중첩 if 문 있는지 확인해보자. msa


- Object, component, class, instance, member function, member val, method, 프로퍼티, attribute(= annotation), field
	- 프로퍼티: 외부에 노출해도 되는 멤버, java 지원x. 하지만, getter, setter 지원..
	- field : 외부에 노출되면 안되는 멤버
	
- 참조형 변수는 메모리 4B. 기본형 변수는 형에 따라 달라지지만 참조형 변수는 보통 4B. 

- abstract vs implement

- static: 정적. 메모리에 올라갈때 고정되는 값.

- 메모리 재할당은 비용이 큼.
- java 는 모든 메소드에 디폴트로 vtable 이 적용되어있음 = 가상함수
	상속과 연관된 키워드. 
	
- const(상수) vs final vs #define
	const 는 컴파일타임에 값을 바꿈. 변수개념이 아님. const a =1 이 컴파일 때 a 를 1로 치환함. 상수만 가능. 기본형에서만(int, long 등등)
	final 변수를 만들고 값 초기화. 참조변수 가능( new ~~()) . 


- 좋은 개발자상
개발할 때는 꼭 사용자의 입장으로 다른 클래스를 사용한다고 쓰자. 의존성 떨어트리기 대작전

// try catch 여러개 말고 에러처리기찾아보기
- 일급객체
	함수를 변수에 할당(assignment)할 수 있다.
	함수를 다른 함수를 인자(argument)로 전달 받는다.
	함수를 다른 함수의 결과로서 리턴될 수 있다.
	
- timestamp 는 4byte 임. 그래서 2038 년에 마지막 날짜임..
	8byte timestamp 를 사용하던가 다른걸 사용하든가


책 2궈 다가져가야함

- 날짜 차이를 비교할 때는 timestamp (즉 초로 전부 단위맞춰서) 로 하면안됨. 뭐 1000 * 60 * 60 * 24 이런거. 시간차이는 상관없음.
- utc, timezone, gmt

- Date() 는 시차를 표현할 수 없음. LocalDate 로 바꾸는게.. 국가별로 고려를 안해주는 날짜 값임.
- db timestamp type도 이거 쓰면 안될듯..

- db 자동 증가 sequence 는  max 는 뭘까. 

- 가비지 컬렉션
	옛날 방식) array처럼 쭉 일자형식
	메모리: eden, s0, s1, Tenured, Permanent
	모든 인스턴스는 eden 에 먼저 올린다.

	eden :eden 이 꽉 찰 때 언제 가비지 컬렉터가 동작. eden와 s0 에서만 gc 동작.
	
	s0: gc 동작하고 남은 메모리는 s0 에 전부 옮긴다.
	
	또 차면 eden, s0 gc 동작. 모두 s1으로 이동.
	또 차면 eden, s1 gc 동작. 모두 s0 으로 이동. 이런식으로 반복.
	gc 발생할 때마다 나이를 먹음.
	나이: 나이를 먹어도 안없어진 메모리들을 전역으로 취급. tenured 로 이동한다.
	
	jvm 튜닝: eden 혹은 tenured 를 늘리는 것.

	g1gc 방식) 그리드 바둑판형식으로 바꿈
	Eden, survivor, old
	메모리가 올라오면 메모리의 상태를 바꿔줌.
	처음 올라오면 eden, 정리하고 살아남으면 survivor, 오래살아남으면 old 
	근데 블럭정리작업들이 필요함. 구멍뚫린 부분이 있으니..
	

- 직렬화: 순환참조 된 객체들 등 이런거 때문에 객체를 연속적인 바이트배열로 바꾸는 것. 멤버변수 하나하나 개발자가 매핑할 필요없이 클래스가 만들어져서 들어감.
- 직렬화 단점: 만약 이기종끼리 직렬화를 하려면 서로 자바프로그램이어야함.
- DB 에서도 저장할 때 직렬화. select 할 때 역직렬화

- 완벽한 트리란. 부모의 depth 가 1차이 나는것.
- iterator 에서 삭제를 하면 문제가 생김. 

- 링크드 리스트는 한 공간 당 8byte 를 더 가진 12byte 임(다음 주소를 가지고 있어야하기 때문에)
- array리스트는 한 공간 당 4byte

- set 은 arrayList 보다 재구성(크기가 바뀔 때) 때 많이 느림. 비용도 많이드림. 초기 크기를 정해주는 게 나음.
	데이터 사이즈를 바꾸면 hashcode 값이 바뀌어서 hash 재정의를 해야함. 
	
- 지네릭
	T 에 올수 있는건 클래스만 됨. int 이런거 기본자료형 이런거 안됨.
	제너릭 클래스 만들거면 extends 쓰는 거 추천


4월달부터 12장 나갈거임. 3월단 교육 잠정중단









- 10장 읽어야함


// class, 메소드 제너릭 ? 안됨? 와일드카드는 클래스 제너릭이나 메서드 제너릭에는 사용할수 없나?
// T super 클래스.class : T 에도 super 를 지원하나. 
// 클래스 제너릭에 super 지원하나?

- 목요일까지 할거. 목요일에 mywebserver 고칠거임. mvc 구조로 만들거임. 12장 열거형, 어노테이션 다시 자세히 읽어오기
// enum class 쓴거 있으면 샘플소스가져오기 -> ApproveFlowStatusCode.class
// 리플레션Reflection 공부해오기. 리플렉션 api 들, 메소드, 어노테이션, 메소드 호출. 파라미터 값 얻어오기 등.

- getDeclaredFields() vs getFields() 차이
- getDeclaredConstructors() vs getConstructors() 차이
- getDeclaredMethod(param) vs getMethod(param) 차이
- getDeclaredField(param) vs getField(param) 차이
전자는 private 포함해서, 상속으로 생긴건 안가져옴|| 후자는 public 만, 상속으로 생긴 변수까지.
